{
  "name": "UserPermission",
  "type": "object",
  "properties": {
    "user_email": {
      "type": "string",
      "description": "E-mail do usu\u00e1rio"
    },
    "modules": {
      "type": "object",
      "description": "Permiss\u00f5es por m\u00f3dulo",
      "properties": {
        "avisos": {
          "type": "string",
          "enum": [
            "none",
            "view",
            "edit"
          ],
          "default": "view"
        },
        "links": {
          "type": "string",
          "enum": [
            "none",
            "view",
            "edit"
          ],
          "default": "view"
        },
        "colaboradores": {
          "type": "string",
          "enum": [
            "none",
            "view",
            "edit"
          ],
          "default": "view"
        },
        "documentos": {
          "type": "string",
          "enum": [
            "none",
            "view",
            "edit"
          ],
          "default": "view"
        },
        "calendario": {
          "type": "string",
          "enum": [
            "none",
            "view",
            "edit"
          ],
          "default": "view"
        }
      }
    }
  },
  "required": [
    "user_email"
  ]
}